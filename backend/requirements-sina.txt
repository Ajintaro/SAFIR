# SAFIR Leitstelle (SINA Rettungsstation) — Minimal-Setup
# ohne LLM, ohne Whisper, ohne PyTorch
# Stand: 2026-04-29
#
# Installation:
#   python -m venv .venv
#   .venv\Scripts\activate   (Windows PowerShell: .venv\Scripts\Activate.ps1)
#   pip install -r requirements-sina.txt
#
# Gesamt-Footprint: ~150 MB (vs. ~4 GB mit torch/whisper)

fastapi==0.135.1
# Starlette 1.0 hat breaking changes in TemplateResponse — der SAFIR-Code
# nutzt die alte API "TemplateResponse(name, ctx)". Pin auf <1.0.
starlette<1.0
uvicorn==0.41.0
httpx==0.28.1
Jinja2==3.1.6
numpy==2.2.6
psutil==7.2.2
python-docx==1.2.0
reportlab>=4.0.0
websockets==16.0
# Optional: PC/SC-Anbindung fuer Omnikey-RFID-Reader. Liefert Native-Wheels
# fuer Windows. Backend laeuft auch ohne — Reader-Loop deaktiviert sich
# automatisch und loggt eine Warnung.
pyscard>=2.0

